
git remote add origin1 git@github.com:ivanjaraiz/ProyectUart.git

::::::::::::::::PROBLEMILLAS::::::::::::::::::::::::::
1. No se tenía la clave actualiza.. ha habido que poner la MAC, irse a manage licence.. ect. 
2. No se pudo generar el bitstream, porque no se tenia el .xdc adjunto al proyecto 
3. Es posible que sea necesario asginar los puertos de la UART, ya que en el I/O Planning no estaban con el click OK. 
4. Acabo de ver un posible problema, se tenía una UART0 conectada al los EMIO, y al aire, eso puede ser un problema, ya que no esta 
asignado a nada. 
5. Se puede manejr el proyecto de VIvado, desde git externamente! los cambios que hago aquí en el bash, se actualizan instantanemente 
en el proyecto en SDK. AMAZING!!!
6.Conectado con el repositorio de Github, podemos tener el proyecto desde el server en privado, y con la llave ssh, 
dando acceso a diferentes usuarios. GOOOOOOD!

Hello world! hecho, para el ejemplo básico de UART
::::

En el ejemplo de polled mode tenemos dos buffer!!! 
donde se mete un patrón en el buffer para TX! se pone la uart en modo localmode! y se espera a recebir el 
patrón en el buffer RX! luego se comparan ambos buffer! y en efecto se ve que el patrón llega. 
Importante, desde modo depuración podemos ver en la misma RAM como van cambiandlo los valores, según la 
dirección de cada buffer, en tiempo REAL! nice!!!
